<?xml version="1.0" encoding="UTF-8"?>
<templates>
    <!-- data-oe-protected and contenteditable are necessary in the blueprint
         because insert will normalize the content and the protected_node_plugin
         is not the first plugin to normalize TODO ABD: check issue when loading
         an article (normalize) -->
    <t t-name="diagram_knowledge_article.EmbeddedDiagram">
        <div>
            <iframe
                    class="o_diagram_editor"
                    t-ref="diagramEditor"
                    t-att-src="url"
                    frameborder="0" />
        </div>
    </t>
</templates>
